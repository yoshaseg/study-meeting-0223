/**
 *
 * newInputValue.
 *
 * @param state
 * @param action
 * @returns {string}
 */
const newInputValue = (state = "", action)=> {
    switch (action.type) {
        default:
            return state;
    }
};

export default newInputValue;